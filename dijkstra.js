function* dijkstra() {
    i = 0;
    selectedNodes = set(nodes[0]);
    focusedNode = nodes[0];
    while (true) {
        // search new mates and set them to selected
        // search min cost and set that node to valid
        // repeat until no more mates
        yield i++;
    }
}